{
  "scripts": {
    "start": "electron app",
    "setup": "npm install && npm run setup:app",
    "setup:app": "(cd app && npm run setup)",
    "build:frontend": "gulp build",
    "watch:frontend": "gulp build:watch",
    "build": "npm run build:frontend && export CSC_NAME=\"Nathaniel Buchar (CAN92U6WU7)\" && build && open -a Finder ./dist/mac"
  },
  "private": true,
  "devDependencies": {
    "browserify": "^13.1.0",
    "dotenv": "^2.0.0",
    "electron-builder": "^5.26.0",
    "electron-prebuilt": "^1.3.3",
    "gulp": "^3.9.1",
    "gulp-if": "^2.0.1",
    "gulp-import-tasks": "^0.1.1",
    "gulp-sass": "^2.3.2",
    "gulp-sequence": "^0.4.5",
    "gulp-sourcemaps": "^1.6.0",
    "gulp-uglify": "^2.0.0",
    "gulp-util": "^3.0.7",
    "vinyl-buffer": "^1.0.0",
    "vinyl-source-stream": "^1.1.0"
  },
  "build": {
    "appId": "com.nathanbuchar.hoodline-mini",
    "category": "public.app-category.news",
    "productName": "Hoodline Mini",
    "compression": "store",
    "mac": {
      "icon": "build/Icon.icns"
    }
  }
}
